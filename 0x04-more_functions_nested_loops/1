#include "main.h"
/**
 *_isdigit - checks for character that are digits
 *@c: character to check
 *
 *
 *Return: 0
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
		retun (1);
	else
		return (0);
}
